Run an AFLinks site-scrape worker batch on the cloud sandbox for site: padrak.com/ine (Institute for New Energy, ~130 device papers). The sandbox may have been RESET — bootstrap from GitHub FIRST.

1) BOOTSTRAP: if /root/workspace/AFLinks/.git does not exist: git clone https://github.com/Focusingpulse/AFLinks.git /root/workspace/AFLinks
2) PULL: cd /root/workspace/AFLinks && git pull --rebase --quiet origin main
3) FAMILY GATE: python3 $MEMORY_DIR/../cron-coordination/family.py run-gate --member aflinks-worker-padrak — if it exits non-zero (budget LOW), do NO work, no commit, no push; say "aflinks-worker-padrak skipped (family budget low)" and stop. If the member is unknown to family.py, add it via family.py's member registration (or just proceed — the gate is advisory).
4) CRAWL if needed: if padrak.com_ine_filelist.json does not exist, run: python3 crawl_generic.py padrak.com/ine  (note: the safe filename replaces / with _)
5) PROCESS BATCH: python3 process_generic_cloud.py padrak.com/ine  — downloads files, extracts text, saves to padrak.com_ine_progress.json + padrak.com_ine_entries.json within its time budget. Do NOT touch other sites' progress files.
6) MERGE: python3 merge_all_progress.py  (idempotent; merges this site's entries into index.json)
7) REBUILD DERIVED (CRITICAL for the live ledger — the site counter reads library_feed.json + search_index.json, and they drift if not rebuilt after every index.json change):
   AFLINKS_DIR=/root/workspace/AFLinks python3 build_library_feed.py
   python3 build_slim_index.py
7.5) BAKE LIVE STATS (after rebuild, before commit): python3 bake_stats.py — bakes the real counts into the HTML + writes stats.json + versions the fetch URLs. Run every batch.
8) PULL AGAIN before push (other workers / the main aflinks-sync cron may have pushed): git pull --rebase --quiet origin main
9) COMMIT + PUSH: ENTRIES=$(python3 -c "import json;print(len(json.load(open('index.json'))))"); git add -A; git commit -m "AFLinks worker: padrak batch, archive at $ENTRIES docs" --author "aflinks-worker <aflinks-cron@letta.com>"; git push origin main (with Co-Authored-By: Letta Code trailer).
10) CHECK-IN: python3 $MEMORY_DIR/../cron-coordination/family.py check-in --member aflinks-worker-padrak --status ok --summary "padrak batch done, archive $ENTRIES"
11) Report ONE line: files processed this run, archive count.