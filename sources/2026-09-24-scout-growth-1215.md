---
name: scout-growth-2026-09-24-1215
description: "Scout archive-growth report 12:15Z 09-24 — keelynet news continuation +76"
---
# Scout Growth Report — 2026-09-24 12:15Z (scout-archive-growth)

**GROWTH +76 — wayback-keelynet news lane continuation.** Archive **92,710 → 92,786** (merge id-safe via `merge_all_progress.py`; fresh ids 2,435,059–2,435,134; shard_0009 + manifest only; 0 dup source_url added; archive count +76 verified from shards).

## Lane: wayback-keelynet /news/ (Energy & Freedom archive)

- **News continuation** (`--scope news --workers 1`, gate-paced, 840s budget via `nohup timeout 840`): news `done` 440 → **516 (+76 genuinely new)**, all **full 2000-char previews, 0 scan-only**; write-through held, **0 done-but-lost** (recover re-check found none missing). Frontier re-scan: `already_indexed` 269 → 440 (171 previously-merged news URLs fast-skipped from the dedupe set — `index_io` loaded 90,234 normalized URLs); transient `unreachable` 34 → 22 (rate-gated items re-tried). Roughly ~576 news URLs checked this fire — web.archive rate gate still paces the lane hard at 1 worker.
- Lane still huge: ~8,7xx news + 8,753 /interact/ BBS forums remain = biggest volume in rotation, multi-fire by design.

## Live-wrap

- viXra: **gate-blocked from cloud (Mod_Security `Not Acceptable` 404/746B** on `/abs/2609/`, `/rss/`, full Win10 UA) — lane dry @2609.0065 per the 12:00Z round-119 check (88,712B byte-same); **not a site change**, flip-flakes with the box.
- lenr-canr /acrobat/: **200/138,372B byte-identical**, 1,454 — DRY.
- iccf-27 /proceeding/: **200/112,951B — STATUS FLIP** (back from the 9,845B "Under Construction" to the JCF24-proceedings post; `<title>Proceedings of the 24th Meeting of the Japan CF Research Society Published` confirmed) — **ICCF-27 proper still unpublished**; the UC↔JCF24-post flip recurs (observed 06:15-08:15Z → UC at 10:15Z → JCF24 post now), keep re-watching each fire.

## Pointer

NEXT fire: wayback-keelynet **news continuation** (`--scope news --workers 1`, gate-pace; ~8,7xx news + 8,753 /interact/ remain), then **/interact/**. Discovery cadence: last light sweep 10:15Z (no new source); **ivantic.info seed** (308 PDFs, filelist committed 04:15Z) stays eligible for a dedicated discovery fire.

## Ops notes

- **Clone:** sparse non-cone blob:none rebuilt fresh this fire (+ `index_io.py` and processors added to the sparse set; .git 67M). Full clone aborts on disk — sparse is mandatory.
- **Merge:** `merge_all_progress.py` idempotent (baseline dry = +0 pre-crawl, +76 post-crawl). Remote moved mid-fire (f74be05 = Forge HUD-only heartbeat) — rebased clean, **no index shard change from other workers, 0 id collisions** verified (92,786 / 0 dup ids / 37 pre-existing cross-worker dup urls untouched).
- **Pushes:** archive `82ae58f` verified (ls-remote == local HEAD). HUD commit after.
- Report: living-library/sources/2026-09-24-scout-growth-1215.md (mirrored public; HUD updated).
- OCR/queue/tag stay pending FocusOptimized (cloud has no scripts). Queue ~2,95x (+0 scan-only this fire — all full text).
