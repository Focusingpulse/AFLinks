# Scout Growth Report — 2026-09-24 00:15Z (scout-archive-growth)

**GROWTH +49 — wayback-keelynet HTML news lane continuation.** Archive **92,093 → 92,142** (commit `094d43a3d`, verified: ls-remote == local + GH API manifest 92,142 @ pushed ref; rebased clean over concurrent `ef6e621fc`). New ids 2,434,442–2,434,490, shard_0009 + manifest only, 0 dup source_url among new.

## Lane: wayback-keelynet /news/ (Energy & Freedom archive)

- `--scope news --workers 1 --limit 200` chunk (~480s budget). Gate cooperated better than 22:15Z: news `done` 68 → 128, **+49 genuinely new entries, all full 2000-char previews, 0 scan-only** (KeelyNet News 2012–2013/2016 pages: free-energy, gravity-control, electronic-health briefs).
- 12 previously-queued `unreachable` retries: partly recovered to done, 18 unreachable now queued for next fire (gate-pace, no ban).
- ⚠ **Write-through gap observed (never-regress):** entries file stopped updating at 00:24:58Z while state kept climbing to 00:28:45Z → 31 done-but-lost. Root cause: Bash-wrapper pipe-kill (stdout piped to `tail`) left the inner `timeout 480` python orphaned; state writes continued, entry writes stopped. **Fix: run crawler with stdout redirected to a file, never through a pipe.** Hand-recovery: small script importing `keelynet_html_crawler.py` with `RECOVER=True` iterating only the done-but-lost keys → **20 rebuilt** (write-through kept entries/progress in sync, 413 total). **11 done-but-lost remain parked → NEXT FIRE MUST run `--scope news --recover` first** (cost ~11 fetches).
- Lane remains huge: ~9,060 news + 8,753 /interact/ BBS forums unprocessed = biggest volume in rotation, multi-fire by design.

## Live-wrap (all dry)

- viXra feed 200/91,273B byte-identical, complete @2609.0061 — DRY
- lenr-canr /acrobat/ 200/138,372B byte-identical, 1,454 — DRY
- iccf-27 /proceeding/ stays 200/9,845B "Under Construction" — TOP CAPTURE TRIGGER HOLDS (proceedings proper still unpublished)

## Discovery (light)

- Web sweep returned only known lanes (lenr-canr, lrdi, lenrgyllc).
- Held candidate **trinitas.ru** verified LIVE (200 via https, RU «Академия Тринитаризма», real academic HTML article archive) — **correction: it is ALREADY a rotation source, COMPLETE (2,777 entries, 09-18)**; the 22:15Z "candidate" note was stale. Hold only as live re-diff candidate.

## Pointer

NEXT fire: wayback-keelynet **news `--recover` FIRST** (rebuild the 11 done-but-lost, ~1-2 min), then continue news lane (`--scope news --workers 1`, gate-pace), then /interact/ 8,753. Discovery cadence: trinitas.ru live re-diff eligible.

## Ops notes

- OCR/queue/tag stay pending FocusOptimized (cloud has no scripts). Queue ~2,955 (+49 news previews OK, 0 new scan-only).
- Full clone (2.1G) rebuilt — sparse-checkout subdir trick failed; used `git sparse-checkout disable` cleanly; stray search_chunk `biology-health-medicine.json` (concurrent-worker artifact) stashed, untouched.
- report: living-library/sources/2026-09-24-scout-growth-0015.md (mirrored to public sources/).
