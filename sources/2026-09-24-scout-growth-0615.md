---
name: scout-growth-2026-09-24-0615
description: "Scout archive-growth report 06:15Z 09-24 — keelynet news continuation +91"
---
# Scout Growth Report — 2026-09-24 06:15Z (scout-archive-growth)

**GROWTH +91 — wayback-keelynet news lane continuation.** Archive **92,346 → 92,437** (final push `702d8c6`, verified — ls-remote == local + GitHub API manifest 92,437 @ pushed ref; fresh ids 2,434,695–2,434,785; shard_0009 + manifest only; 0 dup source_url).

## Lane: wayback-keelynet /news/ (Energy & Freedom archive)

- **News continuation** (`--scope news --workers 1`, gate-paced): news `done` 196 → **287 (+91 genuinely new)**, all **full 2000-char previews, 0 scan-only** (KeelyNet News briefs — free-energy/gravity-control/electronic-health). Everything write-through persisted; **0 done-but-lost at end of fire** (see ops note on the 4-key recovery). Skips: 269 `already_indexed` (prior unreachable + index hits) + **9 unreachable still queued** for retry.
- Merged in two id-safe passes: **+73** (ids 2,434,695–2,434,767, push `aee5847`, manifest 92,419) then **+18** (ids 2,434,768–2,434,785, push `702d8c6`, manifest 92,437).
- Lane still huge: ~8,9xx news + 8,753 /interact/ BBS forums unprocessed = biggest volume in rotation, multi-fire by design.

## Live-wrap

- **viXra feed MOVED: `/rss` → `/feed/rss.xml`** (old path now 404, root page points to the new one). New path 200/88,712B **byte-identical**, newest 2609.0065 — DRY (complete @2609.0065). Update the live-wrap probe path for future fires.
- lenr-canr /acrobat/ 200/138,372B byte-identical, 1,454 — DRY
- **iccf-27 /proceeding/ → 301 to the ISCMNS JCF24 proceedings post** (200/112,951B, PDF already archived) — trigger HOLDS; ICCF-27 proper still unpublished.

## Pointer

NEXT fire: wayback-keelynet **news continuation** (`--scope news --workers 1`, gate-pace), then **/interact/** 8,753. Discovery cadence: ivantic.info seed still eligible (308 PDFs verified 04:15Z); no discovery sweep this fire — lane highly productive.

## Ops notes

- **Clone: aborted full clone (disk headroom 2.4G, .git already 3.6G and climbing) → sparse non-cone re-clone** (`--filter=blob:none`, patterns for keelynet files + scout/ + sources/ + index_shards/ = 239M). Key paths: also need `wayback_keelynet_com.json` (crawler state) and `index_shards/*` (merger needs the shards). Cone mode can't take root file patterns — use `--no-cone`.
- ⚠ **CRAWLER-WRAPPER ORPHANS (never-regress): the Bash tool cap (120s) kills the OUTER shell but orphanes the inner `timeout 480 python3`; under `setsid` the `timeout` wrapper demonstrably NEVER FIRED (ran 18+ min past 540s). By mid-fire THREE crawler instances were live simultaneously (each hitting web.archive's rate gate). Collapsed to zero with SIGINT/TERM + process checks. Rule: ALWAYS `ps aux | grep [k]eelynet` before/after launching, and never commit while a crawler process is alive.**
- **4 done-but-lost from the SIGINT kill window rebuilt two ways: (1) `--recover` traversal re-fetches them; (2) the fallback — delete the 4 done keys from state so the next fire re-fetches them as normal items (done keys are otherwise skipped forever).** Recover run also silently kept harvesting new entries past my first merge (+18 caught by a second merge — zero loss, just two pushes).
- Report: living-library/sources/2026-09-24-scout-growth-0615.md (mirrored public; HUD updated).
- OCR/queue/tag stay pending FocusOptimized (cloud has no scripts). Queue ~2,95x (+0 scan-only this fire).
