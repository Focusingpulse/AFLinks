---
name: scout-growth-2026-09-26-1415
description: "scout-archive-growth fire 14:15Z 2026-09-26 — wayback-keelynet news continuation +91 (archive 95,590→95,681); merge id-safe, 0 collisions."
---

# Scout Archive Growth — 2026-09-26 14:15Z

**GROWTH +91 — wayback-keelynet NEWS continuation.** FINAL archive **95,590 → 95,681** (merge id-safe via `merge_all_progress.py` — dedupe source_url, **0 dup new, 0 id collisions**; fresh ids **2,437,939–2,438,029**; shard_0009 + manifest only; archive push `d8dcda099` verified — **ls-remote == local + GH API manifest 95,681 @ref**).

## Situation
- **Keelynet news lane** — crawler `--scope news --workers 1`: news done **2345 → 2436 (+91 genuinely new**, all full 2000-char previews, 0 short / 0 scan-only); already_indexed frontier fast-skip held 440; unreachable 326 → 340 (web.archive rate-gate paced hard post-frontier). Ran a full 840s budget PLUS a 300s resume window this fire (launcher shell hit the 120s tool timeout but `setsid` kept the crawler alive — no loss, write-through on every entry). New pages are KeelyNet News archive items (`/news/0312*.html`, `/news/0318*.html` etc., 2012/2016 coverage).
- Next: **wayback-keelynet /interact/ lane (8,753 remain)** is the biggest volume in rotation after news.

## Live-wrap (unchanged this slot)
- viXra RSS **200/95,755B byte-identical** → **frontier HOLDS @2609.0075** (0076+ 404/Mod_Security-gated)
- lenr-canr `/acrobat/` 200/138,372B byte-identical (1,454) → DRY
- iccf-27 `/proceeding/` 200/9,845B Under Construction (ICCF-27 proper unpublished — TOP trigger HOLDS)
- lenr.su 200/102,559B byte-identical → DRY

## Discovery
- Not due (news lane still has volume; wayback-keelynet /interact/ 8,753 untouched). **elib.biblioatom.ru** (RU e-library, 200/124,039B) stays next seed candidate for a lane-break fire; cheniere_marukka_ch (252) + faraday_ru (132) standby.

## Ops
- Fresh sandbox → blobless clone rebuilt twice: the first background clone attempt was interrupted by a second clone against the same dir and both died (⚠ parallel-clone hazard — the rotation's known lesson; killed strays, rebuilt, then `git checkout main` full materialization + `checkout-index` for state files).
- Remote moved mid-setup (9e0a9e7 → aadceac, other workers active); no mid-crawl movement this fire.
- Merge id-safe via `merge_all_progress.py` (dry-run +91 only / 70 sites, 0 dup source_url, contiguous ids 2,437,939–2,438,029, 0 collisions).
- **0 id collisions.** Stash-noise → `pull --rebase` (up to date) → pop → push `aadceac..d8dcda099`, verified.
- OCR/queue/tag ~3,4x pending FocusOptimized (cloud has no OCR/queue scripts). POINTER: wayback-keelynet news has more volume; continue `--scope news --workers 1` next fire, then /interact/.
