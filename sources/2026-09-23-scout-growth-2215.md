---
name: scout-growth-2026-09-23-2215
description: "Scout archive growth report 22:15Z 2026-09-23: wayback-keelynet news lane continuation +19 (archive 91,954->91,973, gate-throttled stop)."
---

# Scout Growth Report — 2026-09-23 22:15Z

**Source:** wayback-keelynet news lane (`--scope news`) — Energy & Freedom news archive continuation
**Merged:** +19 entries, id-safe via merge_all_progress.py (archive **91,954 → 91,973**; commit `03da871`)
**Fresh ids:** 2,434,303 – 2,434,321 | **0 dup source_url among new** | shard_0009 + manifest only
**Push verified:** `ls-remote origin main` == local HEAD (`03da871`)

## What happened

- **News lane drove +19 this fire.** Archive gate (web.archive) had cooled since the 20:15Z clamp, so `keelynet_html_crawler.py --scope news --workers 1 --limit 200` was launched in a 440s budgeted chunk. News `done` went **49 → 68** (total entries file 345 → 364; the 19 genuinely new all full-text 2000-char previews, 0 scan-only). Then the IP rate-gate re-throttled hard — an ~3-minute stretch produced zero new entries while 12 `unreachable` skips accumulated (each a 3×-attempted fetch). Per **never-regress** (no ban), the run was **stopped, not retry-dumped**. Those 12 unreachable marks are transient and get retried next fire.
- **Crawl was killed clean mid-chunk** — write-through save means the 19 entries are already persisted in `wayback_keelynet_html_entries.json` + mirrored to progress; the kill caused no data loss (all prior kill-window lessons held).
- ~9,160 news + 8,753 interact remain — still the biggest volume in rotation; multi-fire project.

## Live-wrap

- viXra feed: HTTP 200, 91,273B byte-identical → complete @2609.0061. **DRY**
- lenr-canr `/acrobat/`: HTTP 200, 138,372B byte-identical (1,454 files) → **DRY**
- iccf-27 `/proceeding/`: HTTP 200, **9,845B back to "Under Construction"** (the 20:15Z 112,951B JCF24-post sighting was the redirect target; that PDF is already archived). ICCF-27 proceedings proper remain unpublished. **DRY**

## Discovery pass (light)

Two web_search sweeps (EN LENR/archive + RU torsion/ether) surfaced only known lanes (lenr-canr, vixri.ru, einsteinandtesla — all already rotated/archived). **New held candidate: trinitas.ru** («Академия Тринитаризма» / Institute of Vacuum Physics — Shipov ether-physics publication surface, HTML pages). Not pursued this fire (full hands on news lane); candidate for a future discovery fire.

## Pointer

**wayback-keelynet news CONTINUES next fire (`--scope news --workers 1`, allow gate cooldown first — the 12 `unreachable` marks will retry, expecting ~+19–60/fire when the gate cooperates), then `/interact/` 8,753.** ~9,160 news + 8,753 interact remain = biggest volume in rotation. Discovery cadence: trinitas.ru held candidate; light sweep each fire.

## Notes

- OCR/queue/tag remain pending **FocusOptimized** (Sandra's Windows machine — no OCR scripts in this cloud box); this fire's harvest is all full-text (0 scan-only), cumulative queue unchanged ~2,944.
- web.archive discipline held: 1 worker, ≥1s spacing, hard backoff, stop-not-dump on sustained throttle — no connection-reset ban (cloud curl passes the AIDA gate; python urllib is TLS-blocked).
- Pre-existing index dup source_urls (36, all older rexresearch CoandaEffect-type) = untouched, historical, out of scope this fire.
