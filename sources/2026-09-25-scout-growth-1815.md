---
name: scout-growth-2026-09-25-1815
description: "scout-archive-growth fire 18:15Z 2026-09-25 — keelynet news +76 (94,457→94,533); i-sis worker moved baseline mid-fire, rebased clean."
---

# Scout Archive Growth — 2026-09-25 18:15Z

**GROWTH +76 — wayback-keelynet NEWS lane continuation.** Archive **94,457 → 94,533** (merge id-safe via `merge_all_progress.py`; fresh ids 2,436,806–2,436,881; shard_0009 + manifest only; **0 dup source_url from new entries**; **0 id collisions**; archive push `eff3db3` verified — ls-remote == local + GH API manifest 94,533 @ref).

## Source / lane
- **wayback-keelynet /news/** — `keelynet_html_crawler.py --scope news --workers 1` (840s budget, `nohup timeout 840`, write-through): done **1584 → 1660 (+76 genuinely new**, all full 2000-char previews, **0 short / 0 scan-only**); already_indexed frontier fast-skip held 440; unreachable 196 → 208 + 1 dead (web.archive rate-gate paced hard into long backoffs — ~1,7xx/9,227 targets reached in budget; SIGTERM at budget end, write-through persisted everything). ~7.1xx news targets + 8,753 /interact/ remain — still the biggest volume in rotation.

## Live-wrap (quick probes, all dry)
- viXra `/feed/rss.xml` 200/92,902B byte-identical @2609.0069 → DRY
- lenr-canr `/acrobat/` 200/138,372B byte-identical (1,454) → DRY
- iccf-27 `/proceeding/` 200/9,845B "Under Construction" (flapped back from JCF24-post; ICCF-27 proper still unpublished) → TOP trigger HOLDS
- lenr.su 200/102,559B byte-identical → DRY

## Discovery
- Not due (3rd-fire cadence; last sweep 10:15Z seeded cheniere.marukka.ch). News lane far from exhausted. **faraday_ru** (132 PDFs, `process_generic_cloud.py faraday_ru`) and **cheniere_marukka_ch** (252 doc candidates) standby when the news lane needs a break. QADI /papers/ + swissenschaft.ch/tesla remain 403-gated candidates.

## Ops
- Fresh sandbox → sparse blobless non-cone clone rebuilt (git 2.39: no `--non-cone` flag, so manual non-cone via `.git/info/sparse-checkout` + `--filter=blob:none`; crawler state + index_shards + scout + sources + merge scripts materialized); tokenized push URL set.
- Remote head at fire start was **81e0235 (Steiner +120 → 94,457, GA250-GA252)** — baseline had moved since the 16:15Z fire; crawler dedupe set (91,981 indexed URLs) already covered the +120.
- **Remote moved 1× mid-fire:** i-sis worker `4c629c6` (archive at 94,457) → committed crawler state → rebased clean → merge id-safe → push `eff3db3` verified. **0 id collisions** (contiguous 2,436,805→2,436,806). 36 legacy dups untouched, 0 new.
- OCR/queue/tag ~3,1x pending FocusOptimized (Sandra's Windows machine — cloud has no OCR/queue scripts).

**POINTER: wayback-keelynet news continuation next (`--scope news --workers 1`, gate-pace; ~7.1xx news targets + 8,753 interact remain), then /interact/; faraday_ru + cheniere_marukka_ch standby for a lane break.**
