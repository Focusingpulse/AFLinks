---
name: scout-growth-2026-09-24-2015
description: "Scout archive-growth report 20:15Z 09-24 — keelynet news continuation +77"
---
# Scout Growth Report — 2026-09-24 20:15Z (scout-archive-growth)

**GROWTH +77 — wayback-keelynet news lane continuation.** Archive **93,139 → 93,216** (merge id-safe via `merge_all_progress.py`; fresh ids 2,435,488–2,435,564; shard_0009 + manifest only; 0 dup source_url among new; archive push `ebe615184` verified — ls-remote == local + GH API manifest 93,216 @ pushed ref).

## Lane: wayback-keelynet /news/ (Energy & Freedom archive)

- **News continuation** (`--scope news --workers 1`, gate-paced, 840s budget via `nohup timeout 840`, stdout→file): news `done` 749 → **826 (+77 genuinely new)**, all **full 2000-char previews, 0 scan-only**; write-through held, **0 done-but-lost**; `already_indexed` frontier fast-skip held at 440; transient `unreachable` 56 → 66 (web.archive rate gate pitched hard mid-fire — long backoff stalls on a few items, same pattern as prior fires, retried next fire by design).
- Lane still huge: ~8,4xx news + 8,753 /interact/ BBS forums remain = biggest volume in rotation, multi-fire by design.
- **Clone note:** the persistent AFLinks clone at `/root/workspace/aflinks/check` was **missing** this fire (fresh sandbox) — rebuilt sparse non-cone blob:none (17M .git), full `index_shards/` + `scout/` + `sources/` + crawler/merge scripts in the sparse set. Full index load verified (93,139 baseline, max id 2,435,487) before merge.

## Ops

- **Concurrent remote move handled cleanly:** two remote commits arrived mid-fire (Forge heartbeat `b03721d5` dossier-only + preview cleaner `94994fa` shard_0004-only). Path: committed crawler state + merged shards locally first, `git rebase origin/main` (clean — **no overlap**: theirs shard_0004 previews, mine shard_0009/manifest/wayback), then verified **0 id collisions** (93,216 ids all unique) and **0 dup source_url among new** (36 pre-existing legacy dup groups untouched).
- **Push:** archive `ebe615184` verified (ls-remote == local HEAD + GH API manifest 93,216 @ ref).
- HUD + report mirrored in public repo `scout/` + `sources/`.

## Pointer

NEXT fire: wayback-keelynet **news continuation** (`--scope news --workers 1`, gate-pace; ~8,4xx news + 8,753 /interact/ remain), then **/interact/**. Discovery cadence: faraday.ru seed + ivantic.info eligible for the next 3rd-fire sweep (none this fire — volume lane highly productive; last light sweep 16:15Z).

## OCR note

OCR/queue/tag steps stay pending FocusOptimized (Sandra's Windows machine; cloud has no OCR scripts). Queue ~3,0x (+0 scan-only this fire — all full text).
