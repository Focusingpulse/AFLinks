---
name: scout-growth-2026-09-25-2015
description: "scout-archive-growth fire 20:15Z 2026-09-25 — keelynet news +80 (94,534→94,614); Forge + preview-cleaner moved baseline mid-fire, rebased clean."
---

# Scout Archive Growth — 2026-09-25 20:15Z

**GROWTH +80 — wayback-keelynet NEWS lane continuation.** Archive **94,534 → 94,614** (merge id-safe via `merge_all_progress.py`; fresh ids 2,436,883–2,436,962; shard_0009 + manifest only; **0 dup source_url from new entries**; **0 id collisions**; archive push `8778ac735` verified — ls-remote == local + **GH API manifest 94,614 @ref**).

## Source / lane
- **wayback-keelynet /news/** — `keelynet_html_crawler.py --scope news --workers 1` (840s budget, `setsid timeout 840`): done **1660 → 1740 (+80 genuinely new**, all full 2000-char previews, **0 short / 0 scan-only**); already_indexed frontier fast-skip held **440**; unreachable 208 → 220 (web.archive rate-gate paced hard into long backoffs as usual; ~2,2xx/9,227 targets reached in budget; SIGTERM at budget end, write-through persisted everything). ~7,6xx news targets + 8,753 /interact/ remain — still the biggest volume in rotation.

## Live-wrap (quick probes, all dry)
- viXra `/feed/rss.xml` 200/92,902B byte-identical @2609.0069 → DRY
- lenr-canr `/acrobat/` 200/138,372B byte-identical (1,454) → DRY
- iccf-27 `/proceeding/` 200/9,845B "Under Construction" (ICCF-27 proper still unpublished; flapped between UC and JCF24-post) → TOP trigger HOLDS
- lenr.su 200/102,559B byte-identical → DRY

## Discovery
- Not due (3rd-fire cadence; last 10:15Z seeded cheniere.marukka.ch — 252 doc URLs standby). News lane not exhausted. **faraday_ru** (132 PDFs) and **cheniere_marukka_ch** (252) standby when the news lane needs a break. QADI /papers/ + swissenschaft.ch/tesla remain 403-gated candidates.

## Ops
- Fresh sandbox → sparse blobless non-cone clone rebuilt (git 2.39 manual non-cone via `.git/info/sparse-checkout` + `--filter=blob:none`; crawler state + index_shards + scout + sources + index_io + merge scripts materialized); tokenized push URL set. Full clone killed mid-flight (was going to exceed timeout) → sparse rebuild landed in ~1s.
- **Remote moved 1× mid-fire:** Forge heartbeat `9acf0827d` (20:20Z, +1 dossier, archive static) + preview-cleaner `29967a878` (0 previews changed, archive at 94,534) → committed crawler state → rebased clean onto fresh baseline → merge id-safe → push `8778ac735` verified. **0 id collisions** (contiguous 2,436,882→2,436,883). 54 legacy dups untouched, **0 new**.
- OCR/queue/tag ~3,1x pending FocusOptimized (Sandra's Windows machine — cloud has no OCR/queue scripts).

**POINTER: wayback-keelynet news continuation next (`--scope news --workers 1`, gate-pace; ~7,6xx news + 8,753 interact remain), then /interact/; faraday_ru + cheniere_marukka_ch standby for a lane break.**
