---
name: scout-growth-2026-09-25-0015
description: "scout-archive-growth fire 00:15Z 2026-09-25 — keelynet news +78 (93,416→93,494); discovery hold (seed ready: faraday.ru)."
---

# Scout Archive Growth — 2026-09-25 00:15Z

**GROWTH +78 — wayback-keelynet NEWS lane continuation.** Archive **93,416 → 93,494** (merge id-safe via `merge_all_progress.py`; fresh ids 2,435,765–2,435,842; shard_0009 + manifest only; 0 dup source_url among new; archive push `8ff659b` verified — ls-remote == local + GH API manifest 93,494 @ref).

## Source / lane
- **wayback-keelynet /news/** — `keelynet_html_crawler.py --scope news --workers 1` (840s budget, write-through): done **906 → 984 (+78 genuinely new**, all full 2000-char previews, 0 scan-only; **0 done-but-lost** verified with crawler's own norm_url). already_indexed frontier fast-skip held 440. unreachable 79 → 90 (web.archive rate-gate paced hard into backoffs mid-fire, same as every prior fire; retried next fire). ~8,2xx news + 8,753 /interact/ remain — biggest volume in rotation.

## Concurrent-merge note
- Remote head moved during the fire: Steiner worker pushed +120 (93,296 → 93,416, commit 8b33cd5) plus an i-sis batch + Forge heartbeat (3eb058f). Rebased clean pre-merge (stash crawler state → pull --rebase → pop); merge assigned fresh ids above the new remote max. 0 id collisions.

## Live-wrap (quick probes, all dry)
- viXra `/feed/rss.xml` 200/92,902B byte-identical @2609.0069 → DRY
- lenr-canr `/acrobat/` 200/138,372B byte-identical (1,454) → DRY
- iccf-27 `/proceeding/` 200/112,951B = JCF24-post state → ICCF-27 proper still unpublished, TOP trigger HOLDS

## Discovery (hold)
- Not a 3rd-fire cadence this fire. **faraday.ru lane seeded and verified (132/132 PDFs live) — `faraday_ru_filelist.json` already committed** (426ff76); harvest-ready via `process_generic_cloud.py faraday_ru` when news lane needs a break or volume dips.

## Ops
- Fresh sandbox → sparse non-cone blob:none clone rebuilt (212M .git materialized with index_shards + scout + sources + crawler/merge scripts); tokenized push URL set; full clone aborted earlier as too heavy.
- OCR/queue ~3,1x pending FocusOptimized (Sandra's Windows machine — cloud has no OCR/queue scripts).

**POINTER: wayback-keelynet news continuation next (`--scope news --workers 1`, gate-pace), then /interact/; faraday.ru ready to harvest (`process_generic_cloud.py faraday_ru`, 132 PDFs) when news lane needs a break.**
