---
name: scout-growth-2026-09-24-1815
description: "Scout archive-growth report 18:15Z 09-24 — keelynet news continuation +78"
---
# Scout Growth Report — 2026-09-24 18:15Z (scout-archive-growth)

**GROWTH +78 — wayback-keelynet news lane continuation.** Archive **93,061 → 93,139** (merge id-safe via `merge_all_progress.py`; fresh ids 2,435,410–2,435,487; shard_0009 + manifest only; 0 dup source_url; archive push `480a363` verified — ls-remote == local + GH API manifest 93,139 @main).

## Lane: wayback-keelynet /news/ (Energy & Freedom archive)

- **News continuation** (`--scope news --workers 1`, gate-paced, 840s budget via `nohup timeout 840`, stdout→file): news `done` 671 → **749 (+78 genuinely new)**, all **full 2000-char previews, 0 scan-only**; write-through held, **0 done-but-lost**; `already_indexed` frontier fast-skip held at 440; transient `unreachable` 45 → 56 (web.archive rate gate pitched hard mid-fire — long backoff stalls on a couple of items, same pattern as prior fires, retried next fire by design).
- Lane still huge: ~8,4xx news + 8,753 /interact/ BBS forums remain = biggest volume in rotation, multi-fire by design.

## Ops

- **Clone:** sparse non-cone blob:none rebuilt fresh (full clone would abort on disk; sparse 69M .git). `index_shards/`, `scout/`, `sources/`, crawler + merge scripts in the sparse set.
- **Concurrent remote move handled cleanly:** another worker (Forge i-sis batch) pushed `090e8ab` (+120 → 93,061) mid-fire. Path: committed crawler state first, `git rebase origin/main` (clean — no crawler-file overlap), then merged on the fresh 93,061 baseline → 93,139. **0 id collisions / 0 dup source_url** (contiguous ids 2,435,410–2,435,487 following remote max 2,435,409).
- **Push:** archive `480a363` verified (ls-remote == local HEAD + GH API manifest 93,139 @480a363, 2 files).
- HUD + report mirrored in public repo `scout/` + `sources/`.

## Pointer

NEXT fire: wayback-keelynet **news continuation** (`--scope news --workers 1`, gate-pace; ~8,4xx news + 8,753 /interact/ remain), then **/interact/**. Discovery cadence: faraday.ru seed + ivantic.info eligible for the next 3rd-fire sweep (none this fire — volume lane highly productive).

## OCR note

OCR/queue/tag steps stay pending FocusOptimized (Sandra's Windows machine; cloud has no OCR scripts). Queue ~2,96x (+0 scan-only this fire — all full text).
