---
name: scout-growth-2026-09-25-2215
description: "scout-archive-growth fire 22:15Z 2026-09-25 — keelynet news +75 (94,614→94,689); viXra frontier moved to 2609.0075 (probe next fire); remote static all fire."
---

# Scout Archive Growth — 2026-09-25 22:15Z

**GROWTH +75 — wayback-keelynet NEWS lane continuation.** Archive **94,614 → 94,689** (merge id-safe via `merge_all_progress.py`; fresh ids 2,436,963–2,437,037; shard_0009 + manifest only; **0 new dup source_url** (37 pre-existing legacy dups untouched); **0 id collisions**; archive push `4706e16` verified — ls-remote == local + **GH API manifest 94,689 @ref**).

## Source / lane
- **wayback-keelynet /news/** — `keelynet_html_crawler.py --scope news --workers 1` (840s budget, `setsid timeout 840`): done **1740 → 1815 (+75 genuinely new**, all full 2000-char previews, **0 short / 0 scan-only**); already_indexed frontier fast-skip held **440**; unreachable 220 → 234 (web.archive rate-gate paced hard into long backoffs as usual; ~1,8xx/9,227 targets reached in budget; SIGTERM at budget end, write-through persisted everything). Corpus this fire: KeelyNet News 2012 + 2015 archive pages. ~7,4xx news + 8,753 /interact/ remain — still the biggest volume in rotation.

## Live-wrap (quick probes)
- **viXra `/feed/rss.xml` 200/95,755B — SIZE CHANGED (was 92,902B) and frontier MOVED: RSS now reaches 2609.0075 (was confirmed 2609.0061). ≥14 new version slots (2609.0062–2609.0075) to verify → next-fire viXra probe/capture candidate.** ⚠
- lenr-canr `/acrobat/` 200/138,372B byte-identical (1,454) → DRY
- iccf-27 `/proceeding/` 200/112,951B iscmns JCF24-post page (ICCF-27 proper still unpublished) → TOP trigger HOLDS
- lenr.su 200/102,559B byte-identical → DRY

## Discovery
- Light sweep (2 searches: EN + RU): only already-tracked surfaces surfaced (lenr-canr, vixri full/retired, lrdi_org) — **no new candidates**. cheniere.marukka.ch (252 doc URLs) + faraday_ru (132 PDFs) standby for a lane break; QADI /papers/ + swissenschaft.ch/tesla remain 403-gated.

## Ops
- Fresh sandbox → sparse blobless non-cone clone rebuilt (git 2.39 manual non-cone `.git/info/sparse-checkout` + `--filter=blob:none`; crawler state + index_shards + scout + sources + index_io + merge scripts materialized); tokenized push URL set. Sandbox default Bash timeout (120s) kills long-running watchers — crawler must run `setsid timeout 840` detached; completion via log for DONE_EXIT.
- **Remote STATIC all fire** (no concurrent worker pushes; baseline `467bb54`) → committed crawler state `0d25fd0`, no rebase needed, merge id-safe → push `4706e16` verified. **0 id collisions** (contiguous 2,436,962→2,436,963). 37 legacy dups untouched, **0 new**.
- OCR/queue/tag ~3,2x pending FocusOptimized (Sandra's Windows machine — cloud has no OCR/queue scripts).

**POINTER: wayback-keelynet news continuation next (`--scope news --workers 1`, gate-pace; ~7,4xx news + 8,753 interact remain), then /interact/; viXra frontier moved to 2609.0075 — PROBE before next harvest; faraday_ru + cheniere_marukka_ch standby for a lane break.**
