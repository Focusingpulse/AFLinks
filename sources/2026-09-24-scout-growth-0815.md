---
name: scout-growth-2026-09-24-0815
description: "Scout archive-growth report 08:15Z 09-24 — keelynet news continuation +75"
---
# Scout Growth Report — 2026-09-24 08:15Z (scout-archive-growth)

**GROWTH +75 — wayback-keelynet news lane continuation.** Archive **92,437 → 92,512** (push `3c34c96`, verified — ls-remote == local + GitHub API manifest 92,512 @ pushed ref; fresh ids 2,434,786–2,434,860; shard_0009 + manifest only; 0 dup source_url).

## Lane: wayback-keelynet /news/ (Energy & Freedom archive)

- **News continuation** (`--scope news --workers 1`, gate-paced, 840s budget): news `done` 287 → **362 (+75 genuinely new)**, all **full 2000-char previews, 0 scan-only** (KeelyNet News briefs — free-energy / gravity / electronic-health). Write-through held; **0 done-but-lost** at end of fire. Skips: 269 `already_indexed` + **23 unreachable queued** (14 new this fire — web.archive rate gate was heavy: single-worker run spent long stretches in 5xx backoff, ~2-3 min per stubborn item).
- ⚠ **MERGE-REBASE LESSON (never-regress): a concurrent "preview cleaner" commit (da79296) landed between my merge and push. `git pull --rebase` conflicted on shard_0009 → `git rebase --abort` → `git reset --hard origin/main` — BUT that also reverted the crawler-state files to their committed versions (583 entries), orphaning the +75 harvest into my first commit (4bf61c9). Recovery: `git checkout 4bf61c9 -- wayback_keelynet_com.json wayback_keelynet_html_entries.json wayback_keelynet_html_progress.json`, re-ran the idempotent merge → +75 re-applied clean on the new baseline. Rule: after any reset --hard, restore crawler state from the orphaned commit before re-merging (merge is URL-deduped + id-safe, so a second run is always clean).**
- Lane still huge: ~8,8xx news + 8,753 /interact/ BBS forums = biggest volume in rotation, multi-fire by design.

## Live-wrap

- viXra feed: **200/88,712B byte-identical**, newest **2609.0065** — DRY @2609.0065 (Mod_Security is UA-filtering: "Mozilla/5.0 (Windows NT 10.0; Win64; x64)" works, short UAs get 406).
- lenr-canr /acrobat/ 200/138,372B byte-identical, 1,454 — DRY.
- iccf-27 /proceeding/ 200/112,951B (301 → ISCMNS JCF24 post, PDF already archived) — trigger HOLDS; ICCF-27 proper still unpublished.

## Pointer

NEXT fire: wayback-keelynet **news continuation** (`--scope news --workers 1`, gate-pace), then **/interact/** 8,753. Discovery cadence: ivantic.info seed still eligible (308 PDFs verified 04:15Z); no discovery sweep this fire — lane highly productive.

## Ops notes

- **Clone**: full clone would abort on disk (repo .git ~3.6G+); used the documented **sparse non-cone clone** (`--no-cone --filter=blob:none`, patterns = keelynet files + wayback_keelynet_com.json + index_shards/ + scout/ + sources/ = ~65M .git + blobs on demand). Verify BEFORE each push that the fetcher add-blob mode didn't pull unrelated trees.
- **Crawler wrapper**: `nohup timeout 840 python3 keelynet_html_crawler.py ... > log 2>&1 &` — timeout DID fire this time (exited 08:33:54Z at 14:00) because the outer launcher shell was NOT the tool-capped foreground shell; do NOT use setsid (documented at 06:15: the timeout wrapper never fires under setsid). ALWAYS `ps aux | grep [k]eelynet` before/after; NEVER commit while a crawler is alive (verified 0 orphans pre- and post-merge).
- Report: living-library/sources/2026-09-24-scout-growth-0815.md (mirrored public; HUD updated).
- OCR/queue/tag stay pending FocusOptimized (cloud has no scripts). Queue ~2,95x (+0 scan-only this fire).
