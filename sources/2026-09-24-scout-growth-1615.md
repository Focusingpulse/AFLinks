---
name: scout-growth-2026-09-24-1615
description: "Scout archive-growth report 16:15Z 09-24 — keelynet news continuation +78"
---
# Scout Growth Report — 2026-09-24 16:15Z (scout-archive-growth)

**GROWTH +78 — wayback-keelynet news lane continuation.** Archive **92,863 → 92,941** (merge id-safe via `merge_all_progress.py`; fresh ids 2,435,212–2,435,289; shard_0009 + manifest only; 0 dup source_url; archive push `872bbc9` verified).

## Lane: wayback-keelynet /news/ (Energy & Freedom archive)

- **News continuation** (`--scope news --workers 1`, gate-paced, 840s budget via `nohup timeout 840`, stdout→file): news `done` 593 → **671 (+78 genuinely new)**, all **full 2000-char previews, 0 scan-only**; write-through held, **0 done-but-lost**; ~600+ news URLs checked this fire (`already_indexed` frontier fast-skip 440–485); transient `unreachable` 34 → 45 — web.archive rate gate pitched hard mid-fire (two long backoff stalls, then bursts; same pattern as prior fires, retried next fire by design).
- Lane still huge: ~8,0xx news + 8,753 /interact/ BBS forums remain = biggest volume in rotation, multi-fire by design.

## Live-wrap

- viXra `/feed/rss.xml`: **200/88,712B byte-identical** @2609.0065 — DRY.
- lenr-canr /acrobat/: **200/138,372B byte-identical**, 1,454 — DRY.
- iccf-27 /proceeding/: **200/9,845B "Under Construction"** (title `ICCF-27 Niagara Falls 2026`) — **ICCF-27 proper still unpublished**, re-watch each fire.

## Discovery (light, 3rd-fire cadence)

- **NEW candidate: faraday.ru** — RU/EN «New Energy Technologies» magazine archive (Frolov/Testatika/Kanarev/Storms content), **23 downloadable issues**, content#.html table pages. NOT in repo tree (checked); root 200/2,484B + content10 200/4,643B verified. Seed `faraday_ru_filelist.json` on a dedicated discovery fire.
- jlnlabs.online.fr = mirror of already-harvested jnaudin.free.fr — skip.
- **ivantic.info seed** (308 PDFs, filelist committed 04:15Z) stays eligible for a dedicated discovery fire.

## Pointer

NEXT fire: wayback-keelynet **news continuation** (`--scope news --workers 1`, gate-pace; ~8,0xx news + 8,753 /interact/ remain), then **/interact/**. Discovery cadence: faraday.ru seed + ivantic.info eligible for the next 3rd-fire sweep.

## Ops notes

- **Clone:** sparse non-cone blob:none rebuilt fresh (full clone aborts on disk — .git hit 4.0G and was aborted; sparse 68M). `index_shards/` + `scout/` + `sources/` + crawler/merge scripts in the sparse set.
- **Merge:** `merge_all_progress.py` idempotent (dry-run = +78 only). Concurrent remote push 6642fa8 (preview cleaner, 0 previews changed) → `pull --rebase` clean, no shard conflict; **0 id collisions / 0 dup source_url** (manifest 92,941; new ids contiguous 2,435,212–2,435,289 following prior max 2,435,211).
- **Push:** archive `872bbc9` verified (ls-remote == local HEAD + GH API manifest 92,941 @872bbc9, 5 files).
- HUD + report mirrored in public repo `scout/` + `sources/`.
- OCR/queue/tag stay pending FocusOptimized (cloud has no scripts). Queue ~2,95x (+0 scan-only this fire — all full text).
