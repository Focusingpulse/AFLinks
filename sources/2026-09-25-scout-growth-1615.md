---
name: scout-growth-2026-09-25-1615
description: "scout-archive-growth fire 16:15Z 2026-09-25 — keelynet news +79 (94,258→94,337); preview-cleaner moved baseline mid-fire."
---

# Scout Archive Growth — 2026-09-25 16:15Z

**GROWTH +79 — wayback-keelynet NEWS lane continuation.** Archive **94,258 → 94,337** (merge id-safe via `merge_all_progress.py`; fresh ids 2,436,607–2,436,685; shard_0009 + manifest only; **0 dup source_url from new entries**; **0 id collisions**; archive push `56f6aae` verified — ls-remote == local + GH API manifest 94,337 @ref).

## Source / lane
- **wayback-keelynet /news/** — `keelynet_html_crawler.py --scope news --workers 1` (840s budget, write-through): done **1505 → 1584 (+79 genuinely new**, all full 2000-char previews, **0 short / 0 scan-only**); already_indexed frontier fast-skip held 440; unreachable 183 → 196 (web.archive rate-gate paced hard into long backoffs — ~1,6xx/9,227 targets reached in budget). ~7,6xx news + 8,753 /interact/ remain — still the biggest volume in rotation.

## Live-wrap (quick probes, all dry)
- viXra `/feed/rss.xml` 200/92,902B byte-identical @2609.0069 → DRY
- lenr-canr `/acrobat/` 301→https, 200/138,372B byte-identical (1,454) → DRY
- iccf-27 `/proceeding/` 301→iscmns.org JCF24-post 112,951B (flapped from 9,845B UC; ICCF-27 proper still unpublished) → TOP trigger HOLDS
- lenr.su 200/102,559B byte-identical → DRY

## Discovery
- Not due (3rd-fire cadence; last sweep 10:15Z seeded cheniere.marukka.ch). Light sweep this fire surfaced only already-tracked surfaces (lenr-canr dry, apparentlyapparel + lrdi_org already in repo). News lane far from exhausted. **faraday_ru** (132 PDFs, `process_generic_cloud.py faraday_ru`) and **cheniere_marukka_ch** (252 doc candidates) both standby when the news lane needs a break. QADI /papers/ + swissenschaft.ch/tesla remain 403-gated candidates.

## Ops
- Fresh sandbox → sparse blobless non-cone clone rebuilt (`--no-checkout --filter=blob:none` + sparse-checkout; crawler state + index_shards + scout + sources + merge scripts materialized); tokenized push URL set.
- **Remote moved 1× mid-fire:** preview-cleaner heartbeat `30f5d4b` (+0 archive, 94,258 unchanged) → committed crawler state `bcb414c` → rebased clean on fresh baseline → merge id-safe → push `56f6aae` verified. **0 id collisions** (contiguous 2,436,606→2,436,607). 36 legacy dups untouched, 0 new.
- OCR/queue/tag ~3,1x pending FocusOptimized (Sandra's Windows machine — cloud has no OCR/queue scripts).

**POINTER: wayback-keelynet news continuation next (`--scope news --workers 1`, gate-pace; ~7,6xx news + 8,753 interact remain), then /interact/; faraday_ru + cheniere_marukka_ch standby for a lane break.**
