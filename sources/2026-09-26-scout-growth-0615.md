---
name: scout-growth-2026-09-26-0615
description: "scout-archive-growth fire 06:15Z 2026-09-26 — wayback-keelynet news continuation +75 (archive 95,168→95,243); remote moved mid-fire (Steiner +120, i-sis batch) handled by re-merge on new base; merge id-safe, 0 collisions."
---

# Scout Archive Growth — 2026-09-26 06:15Z

**GROWTH +75 — wayback-keelynet NEWS continuation.** FINAL archive **95,168 → 95,243** (merge id-safe via `merge_all_progress.py` — dedupe source_url, **0 dup new, 0 id collisions**; fresh ids **2,437,517–2,437,591**; shard_0009 + manifest only; archive push `7502220` verified — **ls-remote == local + GH API manifest 95,243 @ref**).

## Situation
Crawler `--scope news --workers 1` (840s budget, `setsid timeout 840`): news done **2044 → 2119 (+75 genuinely new**, all full 2000-char previews, 2 short-ok, 0 scan-only); already_indexed frontier fast-skip held 440; unreachable 273 → 289 (web.archive rate-gate paced hard post-frontier; ~2,1xx/9,227 reached in budget). New pages are KeelyNet News 2012 archive items (`/news/0308*.html`).

## Remote-moved handling
Remote moved **while my crawler ran** (Steiner harvest +120, then i-sis batch — archive 95,048 → 95,168, base fdfa53b → f84244e). The rebase conflicted on `shard_0009.json` / `manifest.json` (both sides appended to the last shard). Resolved by hard-resetting `index_shards/` to origin/main's new base and re-running the idempotent merge (dedupe source_url, fresh ids after remote's new max) → single clean commit `7502220` on top of f84244e. No double-append, no collisions, no lost crawler state (crawler write-through persisted).

## Live-wrap (unchanged this slot)
- viXra RSS 302→200/95,755B byte-identical → **frontier HOLDS @2609.0075** (0076+ Mod_Security-gated)
- lenr-canr `/acrobat/` 200/138,372B byte-identical (1,454) → DRY
- iccf-27 `/proceeding/` 200/9,845B Under Construction (ICCF-27 proper unpublished — TOP trigger HOLDS)
- lenr.su 200/102,559B byte-identical → DRY

## Discovery
- Not due (news lane far from exhausted — ~7,1xx news + 8,753 interact remain; last deep seed cheniere 10:15Z 09-25). **elib.biblioatom.ru** (RU e-library) stays next seed candidate for a lane-break fire.

## Ops
- Fresh sandbox → sparse blobless non-cone clone rebuilt (targeted set: crawler state + all index_shards + scout + sources + index_io + merge scripts; ~20s). Push URL tokenized.
- **0 id collisions** (contiguous 2,437,517–2,437,591).
- OCR/queue/tag ~3,2x pending FocusOptimized (cloud has no OCR/queue scripts). POINTER: wayback-keelynet news continuation next (`--scope news --workers 1`, gate-pace), then /interact/ (8,753 remain).
