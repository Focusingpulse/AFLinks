---
name: scout-growth-2026-09-23-1815
description: "Scout archive growth report 18:15Z 2026-09-23: wayback-keelynet re-diff — HTML first harvest +237 (archive 91,581->91,818)."
---

# Scout Growth Report — 2026-09-23 18:15Z

**Source:** wayback-keelynet re-diff (rotation deep target, index 10)
**Merged:** +237 entries, scoped id-safe append (archive **91,581 → 91,818**, commit `5d1908a`)
**Fresh ids:** 2,433,930 – 2,434,166 | **0 dup source_url** | shard_0009 + manifest only
**Push verified:** `ls-remote origin main` == local HEAD `5d1908a`; GitHub API manifest @pushed ref = **91,818**

## What happened

- **PDF lane confirmed exhausted first:** CDX `.pdf$` (collapse=digest) pool is byte-identical to the 09-07 run — 308 captures / 199 unique originals, all already done/skipped in `wayback_keelynet_com.json`. Re-running `wayback_scavenger.py --domain keelynet.com` = 0 new.
- **Widened the lane to HTML:** CDX `mimetype:text/html` under `keelynet.com` (matchType=domain, collapse=urlkey, statuscode:200) = **18,961 unique pages** — 9,227 `/news/` ("Energy & Freedom" news archive), 8,753 `/interact/` (BBS forums), 842 curated Borderland Research article pages (`/energy/ /gravity/ /biology/ /keely/ /davidson/ /rife/ /tesla/ etc`), 139 junk.
- **New tool `keelynet_html_crawler.py`** (committed): CDX text/html pool → curl `{ts}id_/{url}` fetches → HTML text extraction → entries with `source_url` normalized to `http://www.keelynet.com/...`, `source_site wayback:keelynet.com`, 2000-char previews, real `<title>` headlines; state lives in `wayback_keelynet_com.json` (new `html` key; scavenger's PDF `done/skipped` untouched) + write-through entries (a kill never loses data) + `--recover` rebuild mode.
- **Harvested this fire:** 324 article pages *done* in state; **237 clean entries merged** (6 junk dropped — 404.html, search forms, banner-rates, home index). Corpus: Bedini motor pages, gravity-control articles, energy/biology/keely/davidson/rife/tesla docs, original news briefs. All 237 have text previews (≥60 chars), 0 scan-only.
- **web.archive gate (reality check):** the `id_` endpoint is IP+rate gated from this cloud box. **AIDA blocks Python urllib's TLS fingerprint** (URLError resets) while curl passes; but **bursts ≥2 workers (or repeated CDX+id_ runs) trip a 5xx→connection-reset cooldown** that blocks all fetches for minutes. Sustainable rate ~1 request / few-sec. Stopped hammering at ~324 done to avoid a long ban; the remaining articles + all news/interact pages continue on future fires when the gate cools.

## Live-wrap (all dry as before)

- viXra feed: HTTP 200, 91,273B byte-identical → complete @2609.0061.
- lenr-canr `/acrobat/`: HTTP 200, 138,372B byte-identical (1,454 files) → DRY.
- iccf-27 `/proceeding/`: HTTP 200, 9,845B "Under Construction" (~14wk post-conf) = **TOP CAPTURE TRIGGER HOLDS**.

## Discovery sweep (3rd-fire cadence, light)

All hits were known/covered lanes (lenr-canr, second-physics, lenr.su, single Zenodo/figshare preprints — none archive-scale). Held candidates unchanged: Habr LENR-ТФЭГ (news article), ZH Lin Jinyi preprint (single doc). **No new source seeded this fire.**

## OPS LESSONS (never-regress)

1. **web.archive AIDA gate from this cloud box:** urllib blocked (TLS fingerprint), curl passes, but the endpoint rate-gates bursts hard → always 1 worker / ≥1s spacing / deep backoff; stop rather than retry-dump on sustained 5xx.
2. **Sparse clone + `git reset --hard origin/main` deletes cone untracked files** — lost the crawler/entries/progress once; recovered from the dangling commit and re-merged clean.
3. **Full clone of AFLinks exceeds the 10-min Bash cap** (>5GB with history) — use `git clone --filter=blob:none --no-checkout` + `sparse-checkout set index_shards scout` (finished in 55s); tokenized push URL required.

## Pointer

**wayback-keelynet HTML lane CONTINUES** (curated articles ~324/~842 done; 481 rate-gated for retry; `/news/` 9,227 + `/interact/` 8,753 are the big remaining volume). Next fire: `python3 keelynet_html_crawler.py --scope articles --recover --workers 1` first (rebuilds ~81 done-but-lost entries from killed windows), then continue articles → /news/ chunk. Archive gate may need minutes of cooldown first.

## Notes

- OCR/queue/tag remain pending **FocusOptimized** (Sandra's Windows machine — no OCR scripts in this cloud box); cumulative queue ~2,944.
- 6 junk pages (404 shell, search forms, banner ad rates, home index) dropped from the harvest as non-documents.
