---
name: scout-growth-2026-09-25-1215
description: "scout-archive-growth fire 12:15Z 2026-09-25 — keelynet news +74 (94,109→94,183); concurrent workers moved baseline mid-fire."
---

# Scout Archive Growth — 2026-09-25 12:15Z

**GROWTH +74 — wayback-keelynet NEWS lane continuation.** Archive **94,109 → 94,183** (merge id-safe via `merge_all_progress.py`; fresh ids 2,436,458–2,436,531; shard_0009 + manifest only; **0 dup source_url from new entries**; archive push `0448230` verified — ls-remote == local + GH API manifest 94,183 @ref).

## Source / lane
- **wayback-keelynet /news/** — `keelynet_html_crawler.py --scope news --workers 1` (840s budget, write-through): done **1356 → 1430 (+74 genuinely new**, all full 2000-char previews except 2 short-ok captures); already_indexed frontier fast-skip held 440; unreachable 158 → 170 (web.archive rate-gate paced hard into long backoffs again — entries stalled static for multi-minute stretches; ~1,4xx/9,227 targets reached in budget). ~7,7xx news + 8,753 /interact/ remain — still the biggest volume in rotation.

## Live-wrap (quick probes, all dry)
- viXra `/feed/rss.xml` 200/92,902B byte-identical @2609.0069 → DRY
- lenr-canr `/acrobat/` 200/138,372B byte-identical (1,454) → DRY
- iccf-27 `/proceeding/` 200/9,845B Under Construction → ICCF-27 proper still unpublished, TOP trigger HOLDS

## Discovery
- Not due this fire (3rd-fire cadence; last sweep 10:15Z seeded cheniere.marukka.ch). News lane nowhere near exhausted. **faraday_ru** (132 PDFs, `process_generic_cloud.py faraday_ru`) and **cheniere_marukka_ch** (252 doc candidates, `process_generic_cloud.py cheniere_marukka_ch`) both standby when the news lane needs a break. QADI /papers/ + swissenschaft.ch/tesla remain 403-gated candidates.

## Ops
- Fresh sandbox → sparse non-cone blob:none clone rebuilt (`--no-checkout --filter=blob:none` + read-tree with `/*` minus heavy dirs; root+index_shards+scout+sources+scripts materialized). Tokenized push URL set.
- **Remote moved 2× mid-fire:** Steiner harvest +120 → 94,109 (8eec2f7), then preview-cleaner + Forge heartbeat + i-sis batch (→ fa1669c). Path followed: commit crawler state (40cce21) → pull --rebase clean onto fresh 94,109 baseline → merge on fresh baseline → **0 id collisions** (contiguous 2,436,457→2,436,458). 36 legacy dup urls untouched (all pre-existing rexresearch/legacy), 0 new dups.
- OCR/queue ~3,1x pending FocusOptimized (Sandra's Windows machine — cloud has no OCR/queue scripts).

**POINTER: wayback-keelynet news continuation next (`--scope news --workers 1`, gate-pace; ~7,7xx news + 8,753 interact remain), then /interact/; faraday_ru + cheniere_marukka_ch standby for a lane break.**
